# 项目术语

- **游戏规则 module**：纯 JavaScript 规则，接收当前游戏状态与一帧输入，返回下一状态及本帧事件。
- **运行生命周期 module**：准备、运行、暂停、死亡、通关等一次游戏过程的状态转换；它消费游戏规则事件，并决定页面该显示的弹窗或提示。
- **上榜资格**：击败 10,000 分第一道 Boss 后取得；在死亡或通关结束时允许登记 Player ID。
- **通关排行榜**：浏览器本地保存的前十成绩，按分数降序、同分按用时升序排列。
- **本地成绩存储 adapter**：隔离浏览器 `localStorage` 与测试用内存存储的适配层；负责成绩规范化、排序与截断。
