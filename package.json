{
  "name": "neon-frontier-aircraft-battle",
  "private": true,
  "version": "1.0.0",
  "description": "Development quality checks for the offline Neon Frontier aircraft battle.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "format": "prettier --write index.html README.md CONTEXT.md fighter-catalog.js game-rules.js game-storage.js run-session.js page-presentation.js package.json scripts/*.cjs tests/*.cjs .github/workflows/*.yml 未来优化路线.html",
    "check:format": "prettier --check index.html README.md CONTEXT.md fighter-catalog.js game-rules.js game-storage.js run-session.js page-presentation.js package.json scripts/*.cjs tests/*.cjs .github/workflows/*.yml 未来优化路线.html",
    "check:syntax": "node scripts/check-syntax.cjs",
    "test:unit": "node --test tests/fighter-catalog.test.cjs tests/game-rules.test.cjs tests/run-session.test.cjs tests/game-storage.test.cjs tests/game-audio.test.cjs tests/page-presentation.test.cjs tests/ui-colors.test.cjs",
    "check": "npm run check:format && npm run check:syntax && npm run test:unit"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
